# T004 · 标题规范化和小问题修复

> 优先级：P2（中）
> 类型：修复/统一
> 涉及文件：约 5 个
> 预计工作量：小

---

## 任务目标

修复一批小问题：标题不规范、命名不一致等，让所有 Demo 的元信息统一。

---

## 问题清单

### 1. 标题格式不统一（3个）

| 编号 | 文件夹 | 当前标题 | 问题 | 应改为 |
|------|--------|----------|------|--------|
| D209 | function-plotter | 函数图像绘制器 · 数学 | 缺"景礴"后缀 | 函数图像绘制器 · 景礴 |
| D211 | binary-tree-visualization | 二叉树与数据结构可视化 · 计算机科学 | 缺"景礴"后缀 | 二叉树与数据结构可视化 · 景礴 |
| D217 | fractal-geometry-generator | 分形几何生成器 · 数学 · 景礴 | 中间多了学科名，顺序不对 | 分形几何生成器 · 景礴 |

**统一格式**：`{Demo名称} · 景礴`

学科信息应该放在导航栏的学科标签里，不放在 title 里。

### 2. 检查杂志讲解型的标题规范
抽查 D101-D115 的标题，看看是否都遵循 `{名称} · 景礴` 格式。

### 3. 检查时空叙事型的标题规范
抽查 D301-D307 的标题，确认格式统一。

---

## 操作步骤

1. 先全局搜索所有 Demo 的 `<title>` 标签，列出来看看
2. 把不符合 `{名称} · 景礴` 格式的都改掉
3. 注意不要改到其他地方的文字，只改 `<title>` 标签里的内容

---

## 注意事项

1. 只改 `<title>` 标签里的内容，不要动页面上显示的标题
2. 页面内显示的 Demo 标题是给用户看的，可以带学科标签，但浏览器标签页的 title 要统一
3. 改完后确认没有破坏任何功能

---

## 验收标准

- [ ] 所有 49 个 Demo 的 title 格式统一为「{名称} · 景礴」
- [ ] 没有出现功能异常

---

*最后更新：2026-08-07*
