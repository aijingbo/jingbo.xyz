# 课件库全量整理 — 新窗口执行提示词

> 将此文件的全部内容粘贴到新窗口的对话框中即可开始执行。

---

## 任务

将所有 demo 整理录入课件库页面（`demos.html`），确保课件库展示全部可用 demo，并完成全量整理。

## 项目信息

- 项目目录：`/Users/a1/Desktop/jingbo.xyz`
- 课件库页面：`demos.html`
- 设计风格：科学杂志风，赤陶暖调（主色 #c2410c，背景暖米白 #faf8f5，文字深炭灰）
- 导航栏仅保留三个核心链接：课件库、技能库、关于
- 网站名称：景礴学院

## 现状

- `demos/` 目录下有约 75 个 demo，每个是一个子目录（含 `index.html`），另有 2 个独立 HTML 文件
- `demo/` 旧目录下有 10 个早期 demo（`01-几何证明题` 到 `10-音乐理论`），需评估是否迁移或废弃
- 当前 `demos.html` 只收录了部分 demo，需要全量补齐
- 已有 `demos/templates/` 目录包含 4 种框架模板

## 执行步骤

### 第一步：盘点

1. 遍历 `demos/` 下所有子目录，读取每个 demo 的 `index.html`，提取：
   - `<title>` 标签内容作为标题
   - 页面中的学科分类信息
   - 页面简介/描述文字
   - 使用的框架类型（沉浸式全屏型 / 杂志讲解型 / 交互工具型 / 时空叙事型）
2. 读取 `demos/` 下的 2 个独立 HTML 文件（`C1-methane-combustion.html`、`M1-solid-geometry.html`）
3. 读取 `demo/` 旧目录的 10 个 demo，与 `demos/` 中的进行对比去重
4. 生成一份完整的 demo 清单（Markdown 格式），包含：编号、目录名、标题、学科、框架类型、简介

### 第二步：整理

1. 对于 `demo/` 旧目录中与 `demos/` 重复的，标记为废弃
2. 对于 `demo/` 旧目录中独有的、有价值的 demo，建议迁移到 `demos/` 并统一英文短横线命名
3. 为每个 demo 确定准确的学科分类（数学、物理、化学、生物、历史、地理、计算机、艺术/音乐、经济、语文、天文）

### 第三步：更新课件库页面

1. 读取当前 `demos.html`，理解现有结构和设计风格
2. 将全部 demo 按学科分组录入课件库
3. 每个卡片包含：
   - 学科标签（小标签，赤陶色）
   - demo 标题
   - 一句话简介（15-20字）
   - 框架类型标识
   - 「进入课件」链接（指向 `demos/[目录名]/index.html`）
4. 保留现有功能：搜索、学科筛选、分页
5. 卡片设计遵循现有杂志目录风格，赤陶暖调
6. 响应式布局，移动端友好
7. 统计区域显示：总课件数、学科数等

### 第四步：生成索引文件

在项目根目录生成或更新 `项目导航.md`，包含：
- 全部 demo 的目录索引（按学科分组）
- 每个 demo 的路径、标题、学科、简介

## 设计要求

- 配色：主色赤陶色 #c2410c，背景暖米白 #faf8f5，文字深炭灰 #1a1a1a
- 字体：标题用 Noto Serif SC，正文用 Noto Sans SC，代码用 JetBrains Mono
- 卡片风格：杂志目录式，精致克制，不要 SaaS 感
- 悬浮效果：卡片轻微上浮 + 阴影加深
- 学科标签颜色统一用赤陶色，不用多色

## 交付物

1. 更新后的 `demos.html`（包含全部 demo）
2. 更新后的 `项目导航.md`（demo 索引）
3. demo 清单文档（Markdown 格式，保存到 `docs/` 下）

## 注意事项

- 不要修改 `index.html`（首页）和 `skills.html`（技能库），这些在其他窗口处理
- 不要修改 `demos/` 下的任何 demo 文件本身
- 保持现有 `demos.html` 的 CSS 和 JS 框架，只扩展内容
- 确保所有链接路径正确
